name: Test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: "github.com/portainer"
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version: '1.18'
      - name: Run tests
        run: |
          go test ./...

